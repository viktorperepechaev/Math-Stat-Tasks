# T.11

1) Пусть θ = −10. Сгенерировать выборку размера n = 200 из распределения N(θ, 1). Построить точный доверительный интервал для θ.

2) Повторить пункт 1 для остальных значений θ из множества

Θ = { k / 10 : k = −100, −99, …, 100 }.

3) Построить график, отметив на оси абсцисс значения θ ∈ Θ и изобразив при каждом θ вертикальный отрезок, соответствующий доверительному интервалу, построенному при данном θ. На полученном графике изобразить прямую f(θ) = θ.

4) Повторить пункты 1–3 при n ∈ {5, 10, 100}. Как меняется длина доверительного интервала с ростом n? Подтвердить результаты численного эксперимента аналитически.
